trigger:
  branches:
    include:
      - master
  batch: True

pool:
  name: Default

variables:
  buildConfiguration: 'Release'
  projectFileLocation: 'src/UnitOfWorkPackages\UnitOfWorkRepository.Contract'

steps:

  - task: DotNetCoreCLI@2
    inputs:
      command: 'restore'
      feedsToUse: 'select'
      vstsFeed: '0def8815-55c0-4e3f-af1d-492e533bae39/82a36090-2fb2-42c0-8645-70548d8ba41f'
      projects: '$(projectFileLocation)/*.csproj'
    displayName: 'Restore the project'

  - task: DotNetCoreCLI@2
    inputs:
      command: 'build'
      arguments: '--configuration $(buildConfiguration)'
      projects: '$(projectFileLocation)/*.csproj'
    displayName: 'Build the project'

  - task: NuGetCommand@2
    inputs:
      command: 'pack'
      packagesToPack: '$(projectFileLocation)/*.csproj'
      versioningScheme: 'off'
    displayName: 'Packed the Project'

  - task: NuGetCommand@2
    inputs:
      command: 'push'
      packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg;!$(Build.ArtifactStagingDirectory)/**/*.symbols.nupkg'
      nuGetFeedType: 'internal'
      publishVstsFeed: '0def8815-55c0-4e3f-af1d-492e533bae39/82a36090-2fb2-42c0-8645-70548d8ba41f'